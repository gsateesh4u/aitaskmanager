# aitaskmanager
Create a web based AI Task Manager application.UI should have a dashboard to view today tasks, it should have calendar view to see tasks , it should have a functionality to create new task, edit a task, delete a task
